import { curryingPartialApplication } from "./currying-partial-application";
import { facade } from "./facade";
import { immediateObject } from "./immediate-object";
import { singleton } from "./singleton";

export default {
  curryingPartialApplication,
  facade,
  immediateObject,
  singleton
};
